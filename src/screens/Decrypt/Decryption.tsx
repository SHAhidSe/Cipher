import React from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";
import DecryptionComponent from "../../components/Encryption/DecryptionComponent";

const Decryption = (): JSX.Element => {
    return (
        <View>
            <DecryptionComponent />
        </View>
    )
}
export default Decryption;